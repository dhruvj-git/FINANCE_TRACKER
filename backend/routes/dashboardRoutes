// backend/routes/dashboardRoutes.js (Reverted File)

const express = require('express');
const router = express.Router();

// Only import the summary function
const { getDashboardSummary } = require('../controllers/dashboardController');
const authenticate = require('../middleware/authMiddleware'); // Verify path

// Only include the GET route for summary data
router.get('/', authenticate, getDashboardSummary);

// POST route for affordability removed

module.exports = router;